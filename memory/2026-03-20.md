## 01:32 完成：SWARM-T005 dispatch.sh 调用兼容性与 prompt-file 链路修复
- 修改 `skills/coding-swarm-agent/scripts/dispatch.sh`：兼容旧的单字符串命令与新的 argv 调用；`--prompt-file` 统一改为 stdin 传递；`mktemp` 改为 macOS/Linux 都稳定的无后缀模板创建方式；保留 Claude JSON sidecar、tee、on-complete 原链路。
- 同步更新 `skills/coding-swarm-agent/SKILL.md`、`references/prompt-codex.md`、`references/prompt-cc-review.md`、`references/prompt-cc-frontend.md`，全部示例统一为推荐的 `--prompt-file + argv` 写法，并注明整串命令字符串仅为向后兼容。
- 验证：`bash -n scripts/dispatch.sh` 通过；隔离 tmux stub 测试确认单字符串调用、argv 调用都能成功 dispatch；多行 prompt 通过 stdin 原样保留。
- 遗留问题：本任务范围内无。
