# 2026-03-04

## PolyGo daemon + Telegram通知（已落地）
- 已在 `/Users/tongyaojin/code/PolyGo/polygo-daemon` 完成通知链路改造并落盘：
  - 支持 `TELEGRAM_BROADCAST_CHAT_ID` 群推送机会通知。
  - `TELEGRAM_BOT_TOKEN` 已从 env 读取（不再硬编码 null）。
  - TelegramService 支持无 DB 的群广播模式（DB 不可用时仍可推送）。
- 当前运行方式：`tmux` 会话 `polygo-daemon` 持续运行 daemon。
- 当前策略：先验证通知能力，不依赖 DB 落库（Supabase 暂不可用）。

## X 列表监测任务（预测列表）
- 列表链接：`https://x.com/i/lists/1980062937063870784`
- 目标：每 30 分钟读取一次，筛选“可用策略”，过滤八卦/无关内容。
- 用户要求的推送格式：
  - 中文
  - 1~2 句话
  - 附原帖链接
- 新增质量要求（用户明确）：
  - 不要“晒图说赚钱”类噪音
  - 至少包含一个可执行思路/概念，最好有细节
  - 推送时附上策略原理与分析判断

## 本轮测试结果（样本抓取）
- 已抓取约 52 条列表内容做首轮筛选。
- 可用策略主要集中在：
  1) 天气市场实时数据驱动（结算窗口）
  2) 相邻温度区间组合仓位
  3) 跨市场分散与统一风控
  4) 对冲积分/机制风险
  5) 聪明钱/异常大单作为信号层

## X Strategy Scout Skill（新增）
- 已创建独立 skill：`skills/x-strategy-scout`。
- 已落地脚本：
  - `scripts/fetch_list.sh`：Bird 拉取**列表30条 + 推荐流20条**并做抓取去重状态更新。
  - `scripts/filter_push.py`：策略候选过滤 + Telegram 推送 + 已推送去重 + 自动关注作者。
  - `scripts/run_once.sh`：单次执行（拉取 -> 过滤 -> 推送）。
  - `scripts/run_loop.sh`：每 30 分钟循环执行（`INTERVAL_SEC=1800`）。
- 运行状态：tmux 会话 `x-strategy-scout` 已启动并运行（重启后生效）。
- 已实现历史去重：通过 `runtime/push_state.json` 的 `sent_ids` 过滤历史已发内容。
- 新增作者沉淀：通过 `runtime/push_state.json` 的 `followed` 记录已自动关注账号。
- 受限项：Bird 0.8.0 无“加入列表成员”命令，已做 `runtime/list_add_queue.json` 队列，待后续用浏览器/API补齐加列表动作。
