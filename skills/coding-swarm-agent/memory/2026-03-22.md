## 12:18 完成：修复 update-task-status.sh / dispatch.sh 的 H1+H2+H3
- 在 `update-task-status.sh` 为 flock 文件描述符增加 `EXIT` trap，保证同层加锁路径在异常退出时也会释放锁并关闭 FD
- 在 `dispatch.sh` 的 heartbeat 循环里统一复用 `UPDATE_STATUS` 调用，保持 running heartbeat 刷新 `active-tasks.json.updated_at` 的路径一致
- 结果：`bash -n` 通过；临时 `HOME` 下验证了 mark running 会写入 `task.tmux`，`running -> running` heartbeat 会成功刷新且返回 0，非法 `done -> running` 仍返回 2
- 遗留问题：无
